kind: store
name: hours
spec:
  url:
    env: MONGO_URL
  collections:
    packages:
      indexes:
        - unique: name
    users: {}
    projects: {}
    assignments:
      indexes:
        - unique:
            - project
            - package
