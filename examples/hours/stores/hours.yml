kind: store
name: hours
spec:
  collections:
    packages:
      indexes:
        - unique: name
    users: {}
    projects: {}
    assignments:
      indexes:
        - unique:
            - project
            - package
