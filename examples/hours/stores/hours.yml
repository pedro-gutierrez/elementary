kind: store
name: hours
spec:
  url:
    env: MONGO_URL
  collections:
    users:
      indexes:
        - unique: email
    tokens: {}
    packages:
      indexes:
        - unique: name
    users: {}
    projects: {}
    assignments:
      indexes:
        - unique:
            - project
            - package
