kind: store
name: hours
spec:
  url:
    env: MONGO_URL
  collections:
    organizations:
      indexes:
        - unique: name
    users:
      indexes:
        - unique: email
    tokens: {}
    packages:
      indexes:
        - unique: name
    users: {}
    projects: {}
    time: {}
