kind: module
name: organization_owner
spec:
  decoders:
    http:
      check:
        any: object
  update:
    check:
      - when:
          encoder: is_organization_owner
        model: {}
      - cmds:
          stop: unauthorized
