kind: module
name: organization_member
spec:
  decoders:
    http:
      check:
        any: object
  update:
    check:
      - when:
          encoder: is_member
        model: {}
      - cmds:
          stop: unauthorized
