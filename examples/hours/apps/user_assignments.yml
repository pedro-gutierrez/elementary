kind: app
name: user_assignments
spec:
  routes:
    hours:
      post: /api/projects/:id/users
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - user_assignments
    - common_store
    - session
    - http
