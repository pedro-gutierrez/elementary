kind: app
name: organization
spec:
  routes:
    hours:
      get: /api/organizations/:id
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - organization
    - common_store
    - any_role
    - session
    - http
