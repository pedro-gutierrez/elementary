kind: app
name: organizations
spec:
  routes:
    hours:
      get: /api/organizations
      post: /api/organizations
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - organizations
    - common_store
    - any_role
    - session
    - http
