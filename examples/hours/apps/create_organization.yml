kind: app
name: create_organization
spec:
  routes:
    hours:
      post: /api/organizations
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - create_organization
    - common_store
    - session
    - http
