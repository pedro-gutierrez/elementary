kind: app
name: get_package
spec:
  routes:
    hours:
      get: /api/packages/:id
  settings:
    - hours
  filters:
    - cors
    - auth
    - package_context
    - organization_owner
  modules:
    - get_package
    - package_time
    - common_store
    - session
    - http

