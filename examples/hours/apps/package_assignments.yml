kind: app
name: package_assignments
spec:
  routes:
    hours:
      post: /api/projects/:id/packages
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - package_assignments
    - common_store
    - session
    - http
