kind: app
name: package_assignments
spec:
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - package_assignments
    - common_store
    - employer_only
    - session
    - http
