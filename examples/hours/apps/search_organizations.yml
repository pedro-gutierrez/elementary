kind: app
name: search_organizations
spec:
  routes:
    hours:
      post: /api/search/organizations
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - search_organizations
    - common_store
    - session
    - http
