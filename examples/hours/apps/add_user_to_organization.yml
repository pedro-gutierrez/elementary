kind: app
name: add_user_to_organization
spec:
  routes:
    hours:
      post: /api/organizations/:id/users
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - add_user_to_organization
    - common_store
    - any_role
    - session
    - http
