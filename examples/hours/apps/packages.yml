kind: app
name: packages
spec:
  routes:
    hours:
      get: /api/packages
      post: /api/packages
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - packages
    - common_store
    - employer_only
    - session
    - http

