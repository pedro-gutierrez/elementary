kind: app
name: create_project
spec:
  routes:
    hours:
      post: /api/organizations/:id/projects
  settings:
    - hours
  filters:
    - cors
    - auth
    - organization_context
    - organization_owner
  modules:
    - create_project
    - common_store
    - session
    - http
