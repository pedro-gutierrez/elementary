kind: app
name: list_organization_users
spec:
  routes:
    hours:
      get: /api/organizations/:id/users
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - list_organization_users
    - common_store
    - any_role
    - session
    - http
