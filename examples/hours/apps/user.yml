kind: app
name: user
spec:
  routes:
    hours:
      get: /api/users/:id
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - user
    - common_store
    - employer_only
    - session
    - http

