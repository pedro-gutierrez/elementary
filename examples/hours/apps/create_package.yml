kind: app
name: create_package
spec:
  routes:
    hours:
      post: /api/organizations/:id/packages
  settings:
    - hours
  filters:
    - cors
    - auth
    - organization_context
    - organization_owner
  modules:
    - create_package
    - common_store
    - session
    - http
