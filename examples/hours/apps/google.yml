kind: app
name: google 
spec:
  routes:
    hours:
      get: /api/google/:stage
  settings:
    - hours
    - web
    - google
  filters:
    - cors
  modules:
    - google_state 
    - google_token 
    - http
