kind: app
name: add_user_to_project
spec:
  routes:
    hours:
      post: /api/projects/:id/users
  settings:
    - hours
  filters:
    - cors
    - auth
    - project_context
    - organization_owner
  modules:
    - add_user_to_project
    - common_store
    - session
    - http
