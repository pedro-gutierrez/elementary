kind: app
name: add_package_to_organization
spec:
  debug: true
  routes:
    hours:
      post: /api/organizations/:id/packages
  settings:
    - hours
  filters:
    - cors
    - auth
    - organization_context
    - organization_owner
  modules:
    - add_package_to_organization
    - common_store
    - session
    - http
