kind: app
name: users
spec:
  routes:
    hours:
      get: /api/users
      post: /api/users
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - users
    - common_store
    - session
    - http
