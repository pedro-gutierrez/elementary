kind: app
name: list_organizations
spec:
  routes:
    hours:
      get: /api/organizations
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - list_organizations
    - common_store
    - session
    - http
