kind: app
name: user_time
spec:
  routes:
    hours:
      get: /api/users/:id/time
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - user_time
    - common_store
    - session
    - http
