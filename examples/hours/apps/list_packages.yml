kind: app
name: list_packages
spec:
  routes:
    hours:
      get: /api/organizations/:id/packages
  settings:
    - hours
  filters:
    - cors
    - auth
    - organization_context
    - organization_owner
  modules:
    - list_packages
    - common_organization
    - common_store
    - session
    - http
