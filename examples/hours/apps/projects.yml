kind: app
name: projects
spec:
  routes:
    hours:
      get: /api/projects
      post: /api/projects
  settings:
    - hours
  filters:
    - cors
    - auth
  modules:
    - projects
    - common_store
    - session
    - http
