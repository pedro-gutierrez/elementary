kind: app
name: list_organization_packages
spec:
  routes:
    hours:
      get: /api/organizations/:id/packages
  settings:
    - hours
  filters:
    - cors
    - auth
    - organization_context
    - organization_owner
  modules:
    - list_organization_packages
    - common_store
    - session
    - http
