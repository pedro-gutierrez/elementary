kind: app
name: list_projects
spec:
  routes:
    hours:
      get: /api/organizations/:id/projects
  settings:
    - hours
  filters:
    - cors
    - auth
    - organization_context
    - organization_member
  modules:
    - list_projects
    - common_organization
    - common_store
    - session
    - http
