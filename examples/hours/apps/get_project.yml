kind: app
name: get_project
spec:
  routes:
    hours:
      get: /api/projects/:id
  settings:
    - hours
  filters:
    - cors
    - auth
    - project_context
    - organization_member
  modules:
    - get_project
    - common_store
    - session
    - http
