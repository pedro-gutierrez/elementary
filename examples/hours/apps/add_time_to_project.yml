kind: app
name: add_time_to_project
spec:
  routes:
    hours:
      post: /api/projects/:id/time
  settings:
    - hours
  filters:
    - cors
    - auth
    - project_context
    - organization_owner
  modules:
    - add_time_to_project
    - common_package
    - common_store
    - session
    - http
