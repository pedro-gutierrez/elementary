kind: app
name: get_organization
spec:
  debug: true
  routes:
    hours:
      get: /api/organizations/:id
  settings:
    - hours
  filters:
    - cors
    - auth
    - organization_context
  modules:
    - get_organization
    - common_store
    - session
    - http
