kind: app
name: get_organization
spec:
  routes:
    hours:
      get: /api/organizations/:id
  settings:
    - hours
  filters:
    - cors
    - auth
    - organization_context
  modules:
    - get_organization
    - common_organization
    - common_store
    - session
    - http
