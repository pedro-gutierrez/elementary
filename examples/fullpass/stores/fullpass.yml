kind: store
name: fullpass
spec:
  collections:
    - name: places
      indices:
        - key
        - name
        - location
    - name: events
      indices:
        - key
        - name
  indices:
    key:
      - id
    name:
      - name
    location: geo
