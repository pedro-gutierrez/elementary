kind: store
name: fullpass
spec:
  collections:
    - name: places
      indices:
        - name
        - location
    - name: events
      indices:
        - name
  indices:
    name:
      - name
    location: geo
