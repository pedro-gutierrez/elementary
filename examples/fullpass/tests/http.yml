kind: test
name: http
spec:
  steps:
    - title: Then I should have a 200
      expect:
        status: 200
      in: @latest
    - title: Then I should have a 409
      expect:
        status: 409
      in: @latest
    - title: Then I should have a 201
      expect:
        status: 201
      in: @latest
    - title: Then I should have a 404
      expect:
        status: 404
      in: @latest
    - title: Then I should have a 401
      expect:
        status: 401
      in: @latest
    - title: Then I should have a 400
      expect:
        status: 400
      in: @latest
    - title: Then I should get one item
      item:
        first: @latest.body
