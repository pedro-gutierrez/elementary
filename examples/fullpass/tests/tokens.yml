kind: test
name: tokens
spec:
  steps:
    - title: When I forgot my password
      http:
        method: post
        url:
          - @baseUrl
          - /api/passwd/forgot
        headers:
          content-type: application/json
        body:
          email: @user.email
      as: latest
