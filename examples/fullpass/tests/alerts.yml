kind: test
name: alerts
spec:
  steps:
    - title: When I get my alerts
      http:
        method: get
        url:
          - @baseUrl
          - /api/alerts
        headers:
          client-id: mobile
          client-secret: @clients.mobile
          authorization: @token
      as: latest
