kind: port
name: fullpass
spec:
  port: 2100
  apps:
    places:
      http: /api/places
    events:
      http: /api/events
    nearby:
      http: /api/nearby
    asset:
      http: /api/assets/:id
    store:
      http: /api/stores/:name
    test:
      http: /api/tests/:test/:settings
