kind: module
name: password
spec:
  update:
    create:
      model: @
      cmds:
        return: hashed
  encoders:
    hashed:
      user: @user
      hash: plop
      version: @version
      id: @id
