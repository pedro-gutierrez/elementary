kind: entity
name: credentials
spec:
  graph: fullpass
  plural: credentials
  attributes:
    email:
      kind: string
      tags: key
    password:
      kind: string
  relations:
    owner:
      entity: user
      tags: belongs
