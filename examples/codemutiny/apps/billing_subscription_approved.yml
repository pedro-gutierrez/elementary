kind: app
name: billing_subscription_approved
spec:
  debug: true
  routes:
    codemutiny:
      get: "/api/billing/subscription/approved"
  settings:
    - codemutiny
  modules:
    - billing_subscription_approved
    - http
