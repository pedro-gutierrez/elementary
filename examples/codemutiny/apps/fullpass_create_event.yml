kind: app
name: fullpass_create_event
spec:
  routes:
    codemutiny:
      post: "/api/fullpass/events"
  settings:
    - codemutiny
    - named: google
  filters:
    - auth
  modules:
    - fullpass_create_event
    - http