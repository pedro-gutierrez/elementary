kind: app
name: billing
spec:
  routes:
    codemutiny:
      get: "/api/billing/:feature"
  settings:
    - codemutiny
    - web
    - named: paypal
  filters:
    - cors
    - auth
  modules:
    - billing_product
    - billing_subscription_create
    - http
