kind: app
name: store
spec:
  routes:
    codemutiny:
      delete: "/api/stores/:name"
      post: "/api/stores/:name"
  settings:
    - admin
  filters:
    - admin
  modules:
    - store
    - http
