kind: app
name: store
spec:
  debug: true
  routes:
    codemutiny:
      delete: "/api/stores/:name"
      post: "/api/stores/:name"
  settings:
    - admin
  filters:
    - admin
  modules:
    - store
    - http
