kind: store
name: codemutiny
spec:
  url:
    env: MONGO_URL
  collections:
    identities: {}
    tokens:
      indexes:
        - expire: nonce
          after: 5
        - expire: session
          after: 60
