kind: store
name: codemutiny  
spec:
  pool: 10 
  url:
    env: ELEMENTARY_MONGO_URL
  collections:
    identities: {}
    subscriptions: {}
    tokens:
      indexes:
        - expire: nonce
          after: 60
        - expire: session
          after: 3600
