kind: store
name: codemutiny
spec:
  url:
    env: MONGO_URL
  collections:
    identities: {}
    subscriptions: {}
    tokens:
      indexes:
        - expire: nonce
          after: 60
        - expire: session
          after: 3600
