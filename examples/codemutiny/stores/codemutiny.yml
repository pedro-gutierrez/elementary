kind: store
name: codemutiny
spec:
  url:
    env: MONGO_URL
  collections:
    log:
      max: 5000
      size: 10240000
    identities: {}
    subscriptions: {}
    tokens:
      indexes:
        - expire: nonce
          after: 60
        - expire: session
          after: 3600
