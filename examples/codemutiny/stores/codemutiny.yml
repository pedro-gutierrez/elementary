kind: store
name: codemutiny  
spec:
  pool: 5
  url:
    env: MONGO_URL
  collections:
    identities: {}
    subscriptions: {}
    tokens:
      indexes:
        - expire: nonce
          after: 60
        - expire: session
          after: 3600
