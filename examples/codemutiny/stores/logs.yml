kind: store
name: logs 
spec:
  url:
    env: MONGO_URL
  collections:
    log:
      max: 100000
      size: 10240000
      indexes:
        - lookup: kind
        - lookup: name
        - lookup: time
        - lookup: level