kind: stream
name: events_unlocated
spec:
  settings: 
    - codemutiny
  alert:
    channel: events 
    title: "Location for event *{{ title }}* with id `{{ ref }}` could not be found"
    severity: warning