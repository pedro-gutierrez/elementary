kind: stream
name: events_unknown
spec:
  settings: 
    - codemutiny
  alert:
    channel: events 
    title: "Event with id `{{ ref }}` could not be resolved from host `{{ host }}`"
    severity: danger 